public class Main {
    public static void main(String[] args) {
        AddressManager manager= new AddressManager();
        // 전체를 다루는 클래스를 만들어서 구동만 함
        manager.run();
        // 그래서 이걸 런 해줌.
        // 메인이 끝나면 다 끝남.
        // 매니저 런시키기.
    }
}
// 컨트롤 클래스